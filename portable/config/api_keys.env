# =============================================================================
# Configuration des clés API - CyberSec Toolkit Pro 2025
# =============================================================================
# 
# Ce fichier centralise toutes les clés API nécessaires au projet.
# Complétez les champs ci-dessous avec vos clés personnelles.
#
# INSTRUCTIONS :
# 1. Décommentez et complétez les clés que vous possédez
# 2. Le système utilisera automatiquement les clés disponibles
# 3. Si aucune clé n'est fournie, le mode simulation sera activé
#
# =============================================================================

# -----------------------------------------------------------------------------
# 🤖 CLÉS LLM (Large Language Models)
# -----------------------------------------------------------------------------

# Option 1: Clé Emergent universelle (recommandée - accès à tous les modèles)
EMERGENT_LLM_KEY=sk-emergent-36f796aDb6aD972232

# Option 2: Clés API directes (utilisez celle que vous préférez)
# OPENAI_API_KEY=sk-votre-cle-openai-ici
# ANTHROPIC_API_KEY=sk-ant-votre-cle-anthropic-ici
# GOOGLE_AI_API_KEY=votre-cle-google-ai-ici

# Configuration du provider par défaut (openai, anthropic, ou google)
DEFAULT_LLM_PROVIDER=openai
DEFAULT_LLM_MODEL=gpt-4o-mini

# -----------------------------------------------------------------------------
# 💳 AUTRES CLÉS API (Optionnelles)
# -----------------------------------------------------------------------------

# Stripe pour les paiements (service Billing)
# STRIPE_API_KEY=sk_test_votre-cle-stripe-ici
# STRIPE_WEBHOOK_SECRET=whsec_votre-secret-webhook-ici

# Autres intégrations futures
# SENDGRID_API_KEY=SG.votre-cle-sendgrid-ici
# TWILIO_API_KEY=votre-cle-twilio-ici

# -----------------------------------------------------------------------------
# ⚙️ CONFIGURATION AVANCÉE
# -----------------------------------------------------------------------------

# Limite de tokens pour les requêtes LLM
MAX_TOKENS_PER_REQUEST=2000

# Timeout pour les requêtes API (en secondes)
API_REQUEST_TIMEOUT=30

# Activation du cache pour les réponses LLM (true/false)
ENABLE_LLM_CACHE=true

# Mode verbose pour le debug des API (true/false)
API_DEBUG_MODE=false

# =============================================================================
# FIN DE CONFIGURATION
# =============================================================================